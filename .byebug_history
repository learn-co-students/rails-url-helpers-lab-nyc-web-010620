exit
@student
@student.save
@student.active = !@student.active
@student.active
exit
@student
exit
@student
exit
@student
exit
@student
exit
rails s
exit
@student.active
@student.active?
@student.first_name
@student.first_name("John")
@student.name("John")
@student(name: John)
@student
@student.first
@student.1
@student.active
@student.activate
if @student.active? @student.update(active:true) else @student.update(active:false)end
if @student.active? @student.update(active:true) else @student.update(active:false)
if @student.active? @student.update(active:true)
if @student.active? @.student.update(active:true)
      @blog.update(active: true)
    else
      @blog.update(active: false)
if @blog.active?
@student.active?
if @student.active =='false'@
@student.active
@student.activate
@student.id
@sudent.active
@student.id
@student.find_by_id(1)
@student
